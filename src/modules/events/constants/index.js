/**
 * Events Constants
 * Export all events constants
 */
const getEvents = require('./getEvents.constants');
const getEventById = require('./getEventById.constants');

module.exports = {
  getEvents,
  getEventById,
};
